{
  "rules": {
    ".read": true,
    ".write": true,

    "queues": {
      "default": {
        "tasks": {
          // doesn't work with index on
          ".indexOn": ["_state"],

          "$eventid": {
          }
        }
      }
    }
  }
}